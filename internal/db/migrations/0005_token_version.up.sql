-- token_version инвалидирует старые токены при смене пароля: сессия «живёт
-- вечно» для активного пользователя, но сброс пароля владельцем/менеджером
-- немедленно завершает все прежние сессии этого пользователя.
ALTER TABLE users ADD COLUMN token_version INTEGER NOT NULL DEFAULT 0;
