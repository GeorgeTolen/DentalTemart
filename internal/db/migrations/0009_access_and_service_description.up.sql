-- Платный доступ клиник + описание услуги.

-- Срок доступа клиники к платформе (пробный период / оплаченный срок).
-- NULL — доступ бессрочный. Дата в прошлом — клиника «заморожена»: войти можно
-- (чтобы увидеть сообщение о продлении), работать — нет.
ALTER TABLE clinics ADD COLUMN access_expires_at TIMESTAMPTZ;

-- Описание услуги в прайсе: что входит, для каких случаев.
ALTER TABLE services ADD COLUMN description TEXT;
